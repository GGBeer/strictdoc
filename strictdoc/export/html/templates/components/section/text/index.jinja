{# TODO: change to 'text' #}
{%- if section.has_freetext -%}
  {%- for free_text in section.get_freetext() -%}
  <sdoc-section-text>
    {{ renderer.render_free_text(document_type, free_text) }}
  </sdoc-section-text>
  {%- endfor -%}
{%- endif -%}
